[config]
set = false

# The shell's own palette, generated from the wallpaper only: it is what the
# "Automatique" theme shows. Relative paths are resolved against this file's
# directory, so the checkout can live anywhere.
[templates.quickshell]
input_path = "../config/GeneratedColors.json.template"
output_path = "../config/GeneratedColors.json"
